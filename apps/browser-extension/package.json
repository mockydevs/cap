{
  "name": "@cap/browser-extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-shared.mjs",
    "test": "node scripts/build-shared.mjs && vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "jsdom": "^25.0.1",
    "vitest": "^3.2.4"
  }
}
